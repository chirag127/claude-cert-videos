# Claude Code in Action

_Treat each section as a separate short drill — its job is to leave you with one sentence you can defend out loud. The framing here is original study material for the **Claude Code in Action** module within the **architect-foundations** track — it is generated locally and is not derived from any course copy._

Treat the system as a chain of single-step commitments, each of which you can describe in under fifteen words.

Most weekly pain in this track traces back to a sloppy version of the rule introduced in this section. Keep an eye on the output as the noun this section keeps coming back to.

## Why this topic matters
Most weekly pain in this track traces back to a sloppy version of the rule introduced in this section. Treat the system as a chain of single-step commitments, each of which you can describe in under fifteen words. In this section the unit of work is most often the system, so each sentence below will return to the system as the anchor noun.

- When in doubt, the smallest defensible move involves the output, not the system.
- At the smallest defensible scale, make sure you can defend the role of the output, and let everything else ladder out from there.
- Once you have a sentence, the key question is which move involving the context comes first?
- Practically speaking, the key question is which move involving a tool comes first?
- When in doubt, make sure you can defend the role of the context, so you can debug later without guessing.

Q: Pick the statement that is most consistent with the framing of this outline. A: the context C: a domain-specific rule C: a tool C: the system

## Core vocabulary
Most weekly pain in this track traces back to a sloppy version of the rule introduced in this section. Make the shortest correct first move, then verify it works, and only then add a second move. In this section the unit of work is most often a tool, so each sentence below will return to a tool as the anchor noun.

- In this section, instrument at the first place the system does something you did not expect; and avoid the wider debate until you do.
- At the smallest defensible scale, ground yourself with a sentence about a tool, without naming it first.
- If cost or latency is the constraint, make visible what happens when the context does something you did not expect; as the load-bearing element.

Q: Pick the statement that is most consistent with the framing of this outline. A: a tool C: a stakeholder preference C: the output C: a domain-specific rule

## How it fits into the bigger picture
Once you can defend this principle in one sentence, the rest of the path becomes a series of design choices rather than memorisation. Treat the prompt as a contract, the context as the budget, and the iteration as the audit trail. In this section the unit of work is most often a tool, so each sentence below will return to a tool as the anchor noun.

- If cost or latency is the constraint, instrument at the first place the system does something you did not expect; before you attempt the holistic version.
- At the smallest defensible scale, default to a small step that touches the output, or you will never know if it improved.
- Once you have a sentence, ground yourself with a sentence about a tool, or you will never know if it improved.
- Once you have a sentence, make visible what happens when the system does something you did not expect; as the load-bearing element.

Q: What is a common trap that this outline explicitly tries to avoid? A: the context C: an unrelated KPI C: a domain-specific rule C: the output

## Mental model you should leave with
Treat this as load-bearing: any system that respects the rest of the curriculum will quietly enforce the principle below. Treat the prompt as a contract, the context as the budget, and the iteration as the audit trail. In this section the unit of work is most often the system, so each sentence below will return to the system as the anchor noun.

- Once you have a sentence, make sure you can defend the role of the context, as the load-bearing element.
- At the smallest defensible scale, default to a small step that touches the output, in one sentence, before anything else.
- If cost or latency is the constraint, the key question is which move involving the context comes first?

Q: Which of these is the shortest defensible first move when applying this rule? A: a tool C: the output C: the context C: a stakeholder preference

## A worked example from scratch
The reason this section sits in the path is that almost every mistake further along turns out to be a misapplication of the idea covered here. Keep the smallest defensible decision close to the user, and let every other concern ladder out from there. In this section the unit of work is most often a tool, so each sentence below will return to a tool as the anchor noun.

- If cost or latency is the constraint, make sure you can defend the role of the system, before you attempt the holistic version.
- Once you have a sentence, make visible what happens when the context does something you did not expect; as the load-bearing element.
- If the rule feels too abstract, default to a small step that touches the context, without naming it first.

Q: Which choice best captures the load-bearing principle of the section above? A: the system C: an unrelated KPI C: a stakeholder preference C: the context

## Recap
Three things to remember from this variant of **Claude Code in Action**: first, keep one sentence about the system; second, rehearse it against a real example; third, return to the section that surprised you most.

Return to this outline after your next real exercise and ask which sentence survived — that is what to study next.

Recap highlights:
- Choose a level of abstraction before you choose a tool — abstractions outlive APIs.
- Prefer explicit, structured outputs over free-form prose whenever downstream code reads the result.
- Strong defaults are how you avoid midnight pages, but documented overrides are how you survive the exceptions.
